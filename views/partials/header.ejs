<!-- Header Navigation -->
<div class="custom-navbar">
    <nav class="navbar navbar-expand-lg navbar-dark">
      <a class="" href="/">
        <img src="/images/logo.png" alt="TechVerse Logo" class="logo-img">
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
          <% if (!user) { %>
            <!-- Guest View -->
            <li class="nav-item">
              <a class="nav-link" href="/users/new">Sign Up</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/users/login">Login</a>
            </li>
          <% } else { %>
            <!-- Logged-in User View -->
            <li class="nav-item">
              <a class="nav-link" href="/users/profile">Profile (<%= user.firstName %>)</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/events/createpage">New Event</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/users/logout">Log Out</a>
            </li>
          <% } %>
        </ul>
      </div>
    </nav>
  
    <nav class="navbar subnavbarNav" id="subnavbarNav">
      <div class="">
        <ul class="navbar-nav d-flex flex-row">
          <li class="nav-item active">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/events">Events</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/events/categories_events">Categories</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/about">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/contact">Contact</a>
          </li>
        </ul>
      </div>
    </nav>
  </div>
  



  <% if(Errormessages.length > 0) { 
    Errormessages.forEach(message => { %>
        <br>
        <br>
        <div class="error-message"><%= message %></div>
    <% });
 } %>

<% if(Successmessages.length > 0) { 
    Successmessages.forEach(message => { %>
        <br>
        <br>
        <div class="success-message"><%= message %></div>
    <% });
 } %>